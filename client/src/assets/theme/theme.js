import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    themeColor: {
      main: "#63d7b0",
      light: "#8ae5c7",
      dark: "#31d49e",
      contrastText: "#242105",
    },
  },
});

export default theme;
